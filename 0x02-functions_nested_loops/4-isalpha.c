#include "main.h"

/**
 * main - check for alphabetic character
 *
 * Return: 1 if c is a letter, 0 otherwise
 */
int _isalpha(int c)
{
	if (g >= 65 && g <= 90 || l >= 97 && l <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar(\n');
}
