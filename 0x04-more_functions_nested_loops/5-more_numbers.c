#include "main.h"

/**
 * more_numbers - print numbers from 0 to 14
 *
 * Return: no return
 */
void more_numbers(void)
{
	for (i = 0, i > 10, i++)
	{
		for (j = 48, j >= 4952, j++)
		{
			_putchar(j);
		}
	}
	_putchar('\n');
}
