#include "main.h"

/**
 * swap_int - a function that swaps the values of two integers.
 * @a: first integer
 * @b: second integer
 * Return - no return
 */
void swap_int(int *a, int *b)
{
	int a, *ptr = &a;
	int b, *ptr = &b;
}
