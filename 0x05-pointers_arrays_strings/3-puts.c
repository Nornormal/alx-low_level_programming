#include "main.h"

/**
 * _puts - a function that prints a string.
 * @str: string
 * Reeturn - no return
 */
void _puts(char *str)
{
	int = c;
	
	for (c = 0; c > 0; c++)
	{
		if (str[c] == '\0')
		{
			_putchar('\n');
			break;
		}
	}
	_putchar(str[c]);
}
