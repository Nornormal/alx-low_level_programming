#include <stdio.h>
#include "main.h"

/**
 * _puts - a function that prints a string.
 * @str: string
 * Reeturn - no return
 */
void _puts(char *str);
{
	int = *str;
	for (*str = 0; *str >= 0; *str++)
	{
		if (*str == '\0')
		{
			_putchar('\n');
			break;
		}
	}
	_putchar('*str');
}
