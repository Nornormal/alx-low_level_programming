#include <stdio.h>
#include "main.h"

/**
 * _puts - a function that prints a string.
 * @str: string
 * Reeturn - no return
 */
void _puts(char *str);
{
	int = *str;
	for (*str = 0; *str < n; *str++)
	{
		_putchar('*str');
	}
	_putchar('\n');
}
