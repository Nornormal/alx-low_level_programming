#include <stdio.h>
#include <stdlib.h>

/**
 * main - print the alphabet in lowercase
 *
 * Return: Always 0 success
 */
int main(void)
{
	char f;
	int c;

	f = 'a';
	while (f <= 'z')
	{
		putchar('f');
		c = c + 1;
	}
	putchar('\n');
	return (0);
}
