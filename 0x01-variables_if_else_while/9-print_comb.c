#include <stdio.h>

/**
 * main - print combinations of numbers
 *
 * Return: Always 0
 */
int main(void)
{
	int g;

	for (j = 48; j < 58; j++)
	{
		putchar(i);
		if (i != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
